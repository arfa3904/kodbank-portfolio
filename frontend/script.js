// KODBANK Frontend - shared script (register/login use inline script in HTML; dashboard uses its own inline).
// Use credentials: 'include' so the JWT cookie is sent with requests to the backend.
// Backend validates the token and returns balance for /check-balance.

console.log('KODBANK frontend loaded');
